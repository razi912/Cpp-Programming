//Input a number and check whether it is even or odd using if–else.

#include<iostream>
using namespace std;

int main(){
    int num;
    cout<<"enter a number:";
    cin>>num;
    if(num%2==0)
    cout<<"even"<<endl;
    else
    cout<<"odd"<<endl;
}